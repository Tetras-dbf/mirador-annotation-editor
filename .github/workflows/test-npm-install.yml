# test that the NPM package can be installed properly (dependency resolution only)
name: Test NPM Install

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # allows manual triggering

jobs:
  test-install-with-dbf-mirador-latest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      # Install dev dependencies so that build tools (vite, etc.) are available
      - name: Install dev dependencies
        run: npm ci --ignore-scripts

      # Build the dist so the tarball contains the compiled output
      - name: Build plugin
        run: npm run build

      # Pack the PR as an installable artifact (build already done, skip prepare)
      - name: Pack plugin tarball
        run: |
          set -euxo pipefail
          npm pack --ignore-scripts
          ls -la *.tgz

      # Simulate a real consumer install: dbf-mirador@latest + this plugin tgz.
      # Also disable scripts so we only test npm's dependency tree resolution.
      # NB: this fails with a 404 until dbf-mirador has its first publish
      # (tracked in mirador#1) -- that's expected, not a regression here.
      - name: Consumer install test (resolution only)
        run: |
          set -euxo pipefail
          mkdir -p /tmp/consumer
          cd /tmp/consumer
          npm init -y
          npm i --ignore-scripts dbf-mirador@latest $GITHUB_WORKSPACE/dbf-mirador-annotation-editor-*.tgz
